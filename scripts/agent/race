#!/usr/bin/env bash
set -euo pipefail
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)/scripts/agent/lib.sh"

agent_run "Go race detector" go test -race ./internal/compiler ./internal/output
agent_note "ok race detector"
